const { PosterBuilder, Layer, Component, BaseElement, RectElement, CircleElement, TextElement, Badge, Card, Button, Rating, StatCard } = require("./src/index"); console.log("=== FKPoster 快速测试 ==="); console.log("核心模块:", PosterBuilder ? "OK" : "FAIL", Layer ? "OK" : "FAIL", Component ? "OK" : "FAIL"); console.log("基础元素:", RectElement ? "OK" : "FAIL", CircleElement ? "OK" : "FAIL", TextElement ? "OK" : "FAIL"); console.log("高级组件:", Badge ? "OK" : "FAIL", Card ? "OK" : "FAIL", Button ? "OK" : "FAIL"); const pb = new PosterBuilder({ width: 1080, height: 1920 }); console.log("PosterBuilder实例化:", pb.width === 1080 ? "OK" : "FAIL"); const layer = pb.createLayer({ name: "test" }); console.log("Layer实例化:", layer.name === "test" ? "OK" : "FAIL"); const text = new TextElement({ x: 100, y: 100, text: "Hello" }); console.log("TextElement实例化:", text.type === "text" ? "OK" : "FAIL"); const btn = new Button({ text: "测试" }); console.log("Button实例化:", btn.type === "button" ? "OK" : "FAIL"); console.log("\\n=== 测试完成 ===");
