/**
 * 元素模块导出
 */
const { RectElement } = require('./RectElement')
const { CircleElement } = require('./CircleElement')
const { TextElement } = require('./TextElement')
const { ImageElement } = require('./ImageElement')
const { DividerElement } = require('./DividerElement')

module.exports = {
  RectElement,
  CircleElement,
  TextElement,
  ImageElement,
  DividerElement,
}
